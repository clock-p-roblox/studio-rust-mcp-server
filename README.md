# studio-rust-mcp-server

本仓已退出当前 Roblox 联调主线，不再维护 helper2、plugin-mcp2、task-agent、bridge CLI 或 MCP 文档。

当前主线仓库：

```text
K:\roblox_space\clockp-mcp
  helper2 / MCP tools / plugin-mcp2 / Studio command broker

K:\roblox_space\roblox-dev-infra
  bridge CLI / launch-prelaunch / code-sync client / task-agent
```

使用规则：

- 不在本仓恢复 `go-helper`、`plugin-mcp2`、`tools/bridge`、`tools/bridge2`、task-agent 或旧 runtime-log 脚本。
- Roblox 联调操作流程只看 `K:\roblox_space\roblox-dev-infra\README.md` 和 `K:\roblox_space\roblox-dev-infra\skills\roblox-agent\SKILL.md`。
- helper2 / plugin-mcp2 的源码、构建和 MCP 运行时说明只看 `K:\roblox_space\clockp-mcp\README.md`。
